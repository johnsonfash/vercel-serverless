{
  "name": "sleekr-simple-service",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No frontend build required for this serverless setup'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@vercel/node": "latest",
    "typescript": "^5.x",
    "vercel": "latest"
  }
}